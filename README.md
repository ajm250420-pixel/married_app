# Batak Heritage Modern — monorepo scaffold

Project canvas created with a production-ready starter structure for:
- Angular frontend (mobile-first)
- Node.js + Express backend
- MongoDB via Docker Compose
- Nginx reverse proxy
- Ready to push to Git

Included in this canvas:
- docker-compose.yml
- frontend/backend Dockerfiles
- folder structure
- starter source files
- environment examples
- deployment notes

/root
  /frontend
  /backend
  /nginx
  docker-compose.yml
  .env.example
  README.md

(Scaffold ready for iterative expansion.)
